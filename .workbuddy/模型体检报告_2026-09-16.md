# 预判系统体检报告 · 2026-09-16

> 范围：`C:\Users\gedayou\WorkBuddy\2026-08-14-09-01-12` 全域资产通跑 + 静态扫描 + 排版量化
> 方法：91 个脚本语法编译 → AST 硬编码/死代码扫描 → 关键链路 6 项实测计时 → 9/16 晨报逐行去重量化
> 结论：**无 P0 功能性 BUG（91/91 语法通过、链路 6/6 跑通）**，但发现 1 项 P1 数据一致性缺陷、6 项 P1 资源浪费、1 组严重的排版重复（最需处理）

---

## 〇、体检总账

| 类别 | 数量 | 最高级别 | 一句话结论 |
|---|---|---|---|
| ① BUG（B-1~8） | 8 | **P1** | 无崩溃级错误；但存在「同一天两个收盘价」的数据不一致缺陷 |
| ② 重复浪费资源（R-1~6） | 6 | **P1** | 每日新建脚本 42 个（~280KB）是最大结构性浪费；Cookie 401 无谓重试 12 次/期 |
| ③ 排版重复（D-1~6） | 6 | **P0** | 单个信息原子全篇复述 **最高 22 次 / 13 个小节**，报告 3 天膨胀 2.9 倍 |
| ④ 流程不顺（F-1~6） | 6 | P2 | 引擎 cwd 依赖 + 误导性报错；`脚本地图.md` 已落后 42 个脚本 |

> ③ 表格重复按「已实际发生且持续恶化」判定为 P0——它不影响运行，但直接损害报告可用性。

---

## 一、BUG（8 项）

### P1-1 · 同一天出现两个收盘价（数据一致性缺陷）

**实测证据**（同一次体检、相隔约 1 分钟）：

| 调用 | close | pct_chg |
|---|---|---|
| `get_daily_ohlc.py 000001` | **3855.48** | **-0.23%** |
| `forecast_analyze.py 000001 --skip-engine`（内部同样调 `get_daily_ohlc`） | **3854.20** | **-0.26%** |

**成因**：`get_daily_ohlc.py` 无任何缓存，每次调用都实时打腾讯 `fqkline` 拿 60 根日线取末根。盘中（午间档 12:30、盘中档）价格在动，多次调用必然返回不同数字。

**影响**：午间/盘中档报告如果在多个环节调它（已确认实际会调 4-5 次），报告内「现价 / 涨跌幅 / 支撑压力相对现价%」会自相矛盾——**同一份报告印出两组不同的现价**。晨报 8:55 未开盘，暂未暴露。

**修复**：见改进项 **I-4**（当日 OHLC 落盘缓存，同进程/同日复用）。

### B-2（P1）· `skill_patches/chan_signal.py` 是旧版引擎，误恢复会回滚

| 文件 | 大小 | 日期 |
|---|---|---|
| `.workbuddy/skill_patches/chan_signal.py`（补丁副本） | 36,925 | 08-27 |
| `~/.workbuddy/skills/chan-signal__skillhub/scripts/chan_signal.py`（**在用**） | **37,431** | 09-07 |

补丁副本**比在用引擎旧 506 字节**。若有人按「补丁」语义回滚，会把 09-07 的引擎修正丢掉。**建议**：要么刷新补丁副本，要么改名为 `*.bak.20260827` 明确标注历史，要么直接删（引擎本体在技能目录）。

### P1-3 · `update_chains.py` 既失效又被列为「工具」

```python
# update_chains.py:5, 71, 76, 215
with open(r'C:\Users\gedayou\WorkBuddy\2026-08-14-09-01-12\.workbuddy\forecast_chain.json', ...)
# 且全文写死 2026-08-26-close / 2026-08-27-morning
```

4 处硬编码绝对路径 + 全篇日期写死 8/26~8/27，**已是一次性脚本**。但《脚本地图.md》把它列在「工具/维护」表里，与 `check_layout.py`、`check_integrity.py` 同级——存在被误当通用工具调用的风险（且换机后路径必挂）。

### P2-4 · 6 个脚本有未使用导入（死代码）

| 脚本 | 未使用 |
|---|---|
| `forecast_analyze.py` | `datetime` |
| `gen_evening_report.py` | `json`、`datetime` |
| `backtest_divergence.py` / `backtest_macd.py` / `backtest_support_compare.py` | `json` |
| `_audit_static.py`（本次体检新建） | `sys`、`io`、`time` |

### P2-5 · `gen_forecast_svg.py` 兜底日期过时

两处 `'2026-08-31'`（L22 / L238）作为 `levels` 缺失时的兜底。活跃脚本，读不到新数据时会**静默画出 8/31 的图**而不报错。

### P2-6 · `build_range_band.py` / `gen_range_band_html.py` 区间与文件名写死

```python
# build_range_band.py:73
TRADE = [b['date'] for b in bars if '2026-08-21' <= b['date'] <= '2026-09-15']
# gen_range_band_html.py:198
out = os.path.join(PROJ, 'outputs', '上证综指_预判区间带_2026-08-21至09-15.html')
```

标题文字也写死「2026-08-21 至 2026-09-15（18 个交易日）· 预判链 47 条」。下次生成需手工改 4 处。

### P2-7 · `md_to_html_report.py` 无参数时静默改写旧产物

默认参数指向 `outputs/作战报告_晨报_2026-08-27_行业强弱榜演示版.md`。体检中实测无参数执行，**直接覆盖了那份 8/27 演示版 HTML**（内容不变，但属静默写入）。建议改为「无参数即报错退出」。

### B-8（P2）· 回测脚本残留绝对路径

`backtest_v5.py:8` / `backtest_forecast_multiperiod.py:11` 写死 `SKILL = r"C:\Users\gedayou\.workbuddy\skills\chan-signal__skillhub"`，未走 `paths.py`。属归档脚本，优先级低。

---

## 二、重复浪费资源（6 项）

> 编号前缀 **R** = Resource。最高级别 P1。

### R-1 · 每日新建脚本族：42 个脚本 / ~280 KB / 骨架同构

| 命名族 | 数量 | 合计字节 | 代表 |
|---|---|---|---|
| `add_morning_*` | 9 | 86,217 | 09-03 → 09-16 |
| `review_consensus_morning_*` | 7 | 69,329 | 09-08 → 09-16 |
| `add_noon_*` | 5 | 35,178 | 09-01 → 09-08 |
| `review_close_*` | 5 | 33,871 | 08-31 → 09-07 |
| `add_close_*` | 3 | 16,068 | 09-07 → 09-15 |
| `review_consensus_close_*` | 3 | 14,780 | 09-07 → 09-15 |
| `add_evening_*` | 2 | 13,025 | 09-03 / 09-04 |
| `calc_tech_*` | 2 | 5,928 | 09-15 / 09-16 |
| `digest_*` | 2 | 3,960 | 09-15 / 09-16 |
| 零散（intraday / bias_stats / review_morning / review_consensus_noon） | 4 | 26,429 | — |
| **合计** | **42** | **~304 KB** | — |

**性质**：脚本骨架（读链 → 找 pending → 校验 → 组装 dict → 幂等写回 → 打印状态）**逐字相同**，只有 payload（日期、判定文本、点位）在变。行级 Jaccard 之所以只有 `calc_tech` 一对 >0.6，是因为每天内嵌的报告文本差异把相似度冲淡了——**结构重复而非文本重复**，正是应该参数化的典型。

**代价**：每期要「复制上一份 → 改 8-10 个日期字符串 → 改判定文本」，既慢又容易漏改（9/15 共识复盘漏落盘事故即此类）。

### R-2 · 同源函数重复 14 组

| 重复函数 | 涉及脚本 | 说明 |
|---|---|---|
| `_get` `dkline` `mkline` `ema` `macd` `ma`（6 个） | `calc_tech_0915.py` ↔ `calc_tech_0916.py` | **行集合 Jaccard 0.85**，几乎全同 |
| `norm` `txt` `gname` `ttime`（4 个） | `digest_0916.py` ↔ `digest_zsxq.py` | 旧版未删，新版复制 |
| `_cookie` `in_window`（2 个） | `fetch_zsxq.py` ↔ `fetch_zsxq_fallback.py` | 《脚本地图》已记「可抽公共模块」，未做 |
| `X()` | `gen_dims_html.py` ↔ `gen_range_band_html.py` | 绘图辅助函数 |
| `load_df()` | `backtest_v5.py` ↔ `backtest_forecast_multiperiod.py` | 归档脚本，可不管 |

`calc_tech_0915.py` 是**纯冗余**——已被 `calc_tech_0916.py` 完全覆盖，可直接归档。

### P1-3 · OHLC 反复拉取（流程文档自认「4-5 次」）

`get_daily_ohlc.py` 每次调用 → 1 次 HTTP → 拉 60 根日线（实际只用末根 + 倒数第二根的 close）。无缓存。除浪费外，还直接引发 **BUG P1-1**。

### P1-4 · Cookie 401 无谓重试：每期 12 次无效请求

`_zsxq.log`（9/16）实证：

```
[cookie-err 48841181481248] try0: HTTP Error 401: Unauthorized
[cookie-err 48841181481248] try1: HTTP Error 401: Unauthorized
[cookie-err 48841181481248] try2: HTTP Error 401: Unauthorized
...4 个星球 × 3 次 = 12 次
```

**401 是鉴权失败，不是瞬时故障**——重试必然同样失败。已连续 4 期（9/14 晨、9/15 晨、9/15 复盘、9/16 晨），累计浪费 48 次无效请求。**应 fail-fast**：401 立即放弃该通道并只提示一次，只有 5xx / 超时才重试。

### P1-5 · 引擎产物 130+ 文件从未清理

`~/.workbuddy/skills/chan-signal__skillhub/output/` 现含 08-14 至 09-16 全部产物，每天新增 5 个（`chansignal.json` / `买卖点对比表.md` / `买卖点面板.html` / `BOLL缠论交叉验证.md` / `boll_chan.json`），**~130 个文件无任何轮转**。仓库里为图片写了 `cleanup_images.py`，引擎产物却没人管。

### R-6 · 三个复盘脚本未合并（流程文档自认）

`review_*` / `review_consensus_*` / `bias_stats_*` 三套逻辑，每档跑三遍读同一条链。合并为 1 个 `review.py --tier X --date Y` 可省两次文件读写与两次 LLM 往返。

---

## 三、排版重复（6 项 · **本项为 P0**）

> 编号前缀 **D** = Duplicate。全部 6 项判 P0：不影响运行，但直接损害报告可用性且持续恶化。

### 体积趋势：3 天膨胀 2.9 倍

| 报告 | 字节 | 行 | 字数 |
|---|---|---|---|
| 09-11 | 21,462 | 315 | 11,934 |
| 09-14 | 34,010 | 377 | 17,779 |
| 09-15 | 41,368 | 386 | 20,890 |
| **09-16** | **61,541** | **433** | **30,242** |

### D-1（P0）· 信息原子全篇复述最高 22 次 · 13 个小节

| 信息原子 | 出现处数 | 横跨小节数 |
|---|---|---|
| **3852 双向决策位** | **22** | **13** |
| **15F55 = 3885.90 核心压制** | **20** | **13** |
| 诱多 / 暴力阳线（DRAGON BALL模型） | 16 | 9 |
| 15F 带宽 0.86% 收口 | 12 | 8 |
| 剧本概率 A/B/C | 12 | 9 |
| 三周期 DIF 同向向下 | 10 | 5 |
| WTI 106 美元 | 9 | 9 |
| 缩量 4.41 亿手 | 9 | 6 |
| FOMC 加息 92% | 6 | 6 |
| 10Y 破 5.00% | 5 | 5 |
| 右侧思维 = 60F55 3935.28 | 5 | 5 |

对照《报告逻辑排版完整性规范_v2·二·去重原则》——**「同一个信息原子全篇只完整落地一次，下游用短引用/指针」**。实际执行是复述 5~22 次。规范与产出之间没有任何机器校验，`check_layout.py` 只管「块在不在、顺序对不对」，**不查重复**。

### P0-2 · 「完整预判字段」支撑/压力行 ↔ 「关键位相对现价百分比」表 = 同一数据两种排版

| 位置 | 内容 |
|---|---|
| L268-269（完整预判字段） | 支撑 3861.65(-0.07%) / 3857.58(-0.17%) / **3852.03(-0.32%)** / 3847.09(-0.45%) / 3839.76(-0.63%)；压力 3878.35 / **3885.90** / 3891.48 / 3895.05 / 3905.54 / 3919.21 / 3923.64 / 3935.28 |
| L309-324（关键位相对现价表） | **完全相同的 14 个价位 + 相对现价%**，仅改为表格与降序排列 |

→ 直接重复 **~250-400 字**，可删一处（建议保留表格版，字段行改指针）。

### P0-3 · 「一句话预判」= 下面 6 小节的全文压缩（本期预判说两遍）

第四块「本期预判」共 7 小节 / **4,695 字**：

| 小节 | 字数 | 占比 |
|---|---|---|
| 一句话预判 | 409 | 3.7% |
| 完整预判字段 | 837 | 7.6% |
| v5 打分明细 | 730 | 6.7% |
| 剧本概率与触发条件 | 681 | 6.2% |
| 关键操作纪律 | 682 | 6.2% |
| 开盘后重点观察（4 项） | 486 | 4.4% |
| 关键位相对现价百分比 | 870 | 8.0% |

「一句话预判」名义是摘要，实际把**方向 + 三因子 + 隔夜三压 + DRAGON BALL模型 双向 + 三剧本概率**全说了一遍 → 等于本期预判整体说两遍。

### P0-4 · 「关键操作纪律」6 条 ↔ 「开盘后重点观察」4 项（重叠 3/4）

- 3852 决策位：纪律 3 一次 + 观察 1 一次
- 量能 / 诱多：纪律 2 + 纪律 6 + 观察 3（**三次**）
- 15F55 3885.90：纪律 1 + 纪律 4 + 观察 2

两节合计 1,168 字讲同一组动作。

### P0-5 · 核心主线速览（三行）↔ 第一块·一 主线 = 压缩版紧接完整版

- L8「今日主线」已含：沙特管道 700 万桶/日、3-6 周、利比亚 NOC、柴油 6 美元、WTI 106、10Y 5.00%、30Y 5.37%、FOMC 92%
- L20-25「第一块·一·1」把这些**逐条完整展开**

速览三行是「摘要」，但摘要后面 3 行紧接就是全文。同理 L9「关键分歧」的巴克莱 +18% 与 DRAGON BALL模型 双向，被 L27-31、L258、L274-278、L287-289 反复展开。

> 另：附录 A 的「Cookie 401 连续第 4 期」事实在**报告头 L4 / 附录表 L392-395（4 行）/ 完整性说明 L399** 共出现 6 次。

### D-6（P0）· 第一块粒度失控：中位 124 字/条，最长 398 字

| 版本 | 第一块字数 | bullet 数 | 均字数/条 | >200 字 | >300 字 |
|---|---|---|---|---|---|
| 09-11（83 条窗口） | 1,546 | 16 | 97 | — | — |
| **09-16（44 条窗口）** | **9,372** | **61** | **154** | **17 个** | **4 个** |

规范要求「每条 1-3 行」≈ 40-120 字。**9/16 窗口内只有 44 条，却写出 9,372 字（213 字/条原始条目），占全报告 31.3%**——是报告膨胀的第一贡献者。注意这是**粒度问题而非条数问题**：9/11 有 83 条却只写 1,546 字。

---

## 四、流程不顺（6 项）

1. **引擎 cwd 强依赖 + 误导性报错**：`run_000001_chansignal.py` / `run_000001_boll_chan.py` 必须在技能目录下执行；在项目根执行报 `[Errno 2] No such file or directory: ...\run_000001_chansignal.py`——**文件其实存在**，只是 cwd 不对，报错把人往错误方向引。
2. **`check_integrity.py` 历史告警永久噪声**：每期固定报「2026-08-24 / 08-28 / 08-31 / 09-01 缺 DRAGON BALL模型 归档」。这 4 天在 DRAGON BALL模型 归档机制建立之前，属**已知历史缺口**，应有 known-exceptions 白名单，否则真缺口会被淹没。
3. **`forecast_analyze.run_engine` 靠 stdout 字符串匹配**：`if 'chansignal.json' in line and '已保存' in line: json_path = line.split('已保存:')[-1]`——引擎改一句日志文案就静默返回 `None`（且不报错）。
4. **「跑完回读 status」靠人工自觉**：9/15 共识复盘漏落盘事故的固化手段只是写进文档与记忆，**没有机器强制**。建议并入 `check_integrity.py` 做断言（pending 数 > 1 即告警）。
5. **`脚本地图.md` 停留在 2026-09-02**：未收录 09-03 之后新增的 42 个每日脚本与 `calc_tech` / `digest` / `bias_stats` 等，文档与资产已脱节。它开篇称「梳理 32 个脚本」，实际 `.workbuddy` 下（不含 archive）已有 **91 个**。
6. **环境层**：Bash coreutils 全缺（`ls`/`grep`/`cp`/`mkdir` 均 `command not found`），已固化到用户级 MEMORY.md。属既有约定，不重复处理。

---

## 五、改进清单（待确认后实施）

> 原则：**先低风险高收益，后结构性重构**。涉及删改代码的项，等你点头再动。

### P0 · 建议本周处理（低风险，直接消除报告质量问题）

| 编号 | 改进项 | 动作 | 预期 |
|---|---|---|---|
| **I-1** | 第四块去重 | 「完整预判字段」支撑/压力行改指针（指向关键位表）；「一句话预判」压到 ≤150 字纯结论（三因子明细交 v5 明细表）；「关键操作纪律」与「开盘后观察」合并为一节 | 第四块 6,542 → ~4,300 字（**-34%**） |
| **I-2** | 第一块粒度约束 | 每条 ≤120 字（1-3 行）；超 200 字的条目拆为「主条 + 子条」或只留结论；宏观展开移入第二块 | 第一块 9,372 → ~5,000 字（**-47%**） |
| **I-3** | 给 `check_layout.py` 加「重复检查」 | 新增第 6 类检查：对 ① 关键位数字 ② 核心结论短语 做全篇计数，同一原子出现 >6 次即 WARN，并打印行号 | 把 P0-1 变成机器可拦 |

### P1 · 建议两周内（消除实际风险 + 显著省时）

| 编号 | 改进项 | 动作 | 预期 |
|---|---|---|---|
| **I-4** | OHLC 当日缓存 | `get_daily_ohlc.py` 落 `.workbuddy/_ohlc_cache_YYYY-MM-DD.json`，同日直接复用；加 `--force` 强制刷新 | 消除 BUG P1-1；每期省 3-4 次 HTTP |
| **I-5** | Cookie 401 fail-fast | 401/403 立即终止该通道（只提示 1 次），仅 5xx/超时重试 | 每期省 12 次无效请求 |
| **I-6** | 每日脚本参数化 | 新建 `add_forecast.py --tier {morning,noon,close,evening} --date YYYY-MM-DD --payload payload.json` 与 `review_chain.py --tier X --date Y`（含 forecast + consensus + bias 三合一） | 替代 42 个脚本族 + 3 个复盘脚本；消除漏改日期风险 |
| **I-7** | 清理失效与陈旧副本 | 删/归档 `update_chains.py`（硬编码失效）；`skill_patches/chan_signal.py` 刷新或改名 `.bak`；归档 `calc_tech_0915.py`、`digest_zsxq.py` | 消除误用/误回滚风险 |

### P2 · 可批量处理（整洁度与效率）

| 编号 | 改进项 |
|---|---|
| **I-8** | `check_integrity.py` 加 known-exceptions（08-24/08-28/08-31/09-01 历史 DRAGON BALL模型 缺口）+ 新增「pending > 1」断言 |
| **I-9** | `forecast_analyze.run_engine` 改为读引擎 JSON 的固定输出路径，不解析 stdout |
| **I-10** | 引擎产物轮转：新增 `cleanup_engine_output.py`，仅保留最近 10 个交易日 |
| **I-11** | `md_to_html_report.py` 无参数改为报错退出（不再默认写演示版） |
| **I-12** | 清 6 处未使用导入；`gen_forecast_svg.py` 兜底日期改动态；`build_range_band*` 区间参数化 |
| **I-13** | 更新《脚本地图.md》：91 个脚本全量登记，新增「每日脚本族 → 待参数化」状态标记 |
| **I-14** | 引擎脚本自己 `os.chdir(os.path.dirname(__file__))`，消除 cwd 依赖与误导性报错 |

---

## 六、体检过程留档

| 动作 | 结果 |
|---|---|
| 语法编译（91 脚本） | 91/91 通过 |
| 关键链路实测 | `check_layout` 1.16s ✅ / `get_daily_ohlc` 1.42s ✅ / `gen_forecast_svg` 1.22s ✅ / `md_to_html_report` 1.50s ✅ / `forecast_analyze` 3.45s ✅ / `check_integrity` 1.03s ✅ |
| 双引擎实测 | 引擎 B 6.62s ✅ / 引擎 C 4.24s ✅（**均须在技能目录下执行**） |
| 版面自检（9/16 晨报） | **ERROR 0 / WARN 0** ✅ |
| 报告文件 | `outputs/作战报告_晨报_2026-09-16.md` 61,541 B / 433 行 / 30,242 字 |

**体检脚本**（已加 `.gitignore`，不入库）：`_audit_static.py`、`_audit_layout.py`、`_audit_layout2.py`
**原始数据**：`_audit_static_result.txt`、`_audit_layout_result.txt`、`_audit_layout2.txt`、`_audit_assets.txt`

---

## 附：遗留待办（非本次范围）

- **Cookie 通道连续第 4 期 401**：需更新 `.workbuddy/zsxq_cookie.txt`（该文件不入 git、严禁跨机拷贝）。目前大鹏鸟 / 短评&信息 / 180K / AI 产业链 四球零数据已持续 3 个交易日。
