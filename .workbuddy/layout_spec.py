# -*- coding: utf-8 -*-
"""报告版面规范（机器可读），与《报告逻辑排版完整性规范_v2.md》对应。

结构：
  TIER_ALIAS  文件名关键词 -> 档位 key
  BLOCKS      区块 key -> (中文名, [标题正则...])，正则用 re.search 匹配标题行文本
  SLOTS       档位 key -> {'required': [...], 'optional': [...]}
              required 缺块 = ERROR（硬伤，推导链不完整）
              optional 缺块 = WARN（排版不完整，prompt 要求但常漏）
              顺序以 required + optional 的先后为规范顺序，用于「顺序错位」检查。

被 check_layout.py 引用。修改版面规则只需改本文件。
"""

TIER_ALIAS = {
    '晨报': 'morning',
    '午间': 'noon',
    '盘中': 'intraday',
    '收盘': 'close',
    '复盘': 'review',
}

# key: (中文名, [标题正则...])  —— 正则允许措辞微变，覆盖「中文序数 / 第X块 / 无编号」三种标题写法
BLOCKS = {
    'core':       ('核心主线速览',   [r'核心主线速览']),
    'star':       ('星球信息',       [r'星球信息']),
    'judge':      ('信息判断',       [r'信息判断', r'跨星球观点对比', r'观点对比']),
    'tech':       ('技术分析',       [r'技术分析', r'盘面与技术面']),
    'forecast':   ('预判',           [r'预判']),
    'industry':   ('行业强弱榜',     [r'行业强弱榜', r'行业判断']),
    'bias':       ('偏差统计',       [r'偏差.*统计']),
    'appendix_a': ('附录A·抓取通道', [r'附录\s*A']),
    'appendix_b': ('附录B·星球代号', [r'附录\s*B']),
    'products':   ('产物清单',       [r'产物清单']),
    # —— 复盘档专属 ——
    'actual':     ('盘面实际',       [r'盘面实际']),
    'review':     ('预判复盘',       [r'预判.*复盘', r'复盘']),
    'cognition':  ('关键认知',       [r'关键认知']),
    'nextday':    ('次日衔接',       [r'次日衔接', r'次日.*衔接']),
}

# 某些块内部必须包含的子内容（标题/关键词级），缺失 = ERROR
# 用于防止「块标题在、但内部核心子内容缺失」的「壳空」问题
# key = BLOCKS 的 key；值为 [(子内容中文名, [关键词正则...])]，正则用 re.search 在该块正文内匹配
SUB_REQUIRED = {
    'judge': [
        ('共同观点', [r'共同观点']),
        ('相反观点', [r'相反观点', r'对立观点']),
    ],
    # 预判块内必须内嵌一张走势图（markdown 图片语法指向 svg/png）。
    # 防「产物清单列了 .svg 路径、但正文没真正引用 → HTML 不内嵌 → 图挂了」。
    'forecast': [
        ('走势图', [r'!\s*\[[^\]]*\]\([^)]*\.svg', r'!\s*\[[^\]]*\]\([^)]*\.png']),
    ],
}

SLOTS = {
    # 偏差统计(bias) 是第四块「预判」的子项，规范顺序在行业强弱榜(industry) 之前
    'morning': {
        'required': ['core', 'star', 'judge', 'tech', 'forecast', 'bias', 'industry'],
        'optional': ['appendix_a', 'appendix_b', 'products'],
    },
    # 午间/收盘为「四块」（无行业榜），行业榜仅晨报「五块」——2026-09-08 精简去重后
    'noon': {
        'required': ['core', 'star', 'judge', 'tech', 'forecast', 'bias'],
        'optional': ['appendix_a', 'appendix_b'],
    },
    'intraday': {
        'required': ['core', 'star', 'judge', 'tech', 'forecast', 'bias', 'industry'],
        'optional': ['appendix_a', 'appendix_b', 'products'],
    },
    'close': {
        'required': ['core', 'star', 'judge', 'tech', 'forecast', 'bias'],
        'optional': ['appendix_a', 'appendix_b'],
    },
    'review': {
        'required': ['actual', 'review', 'bias', 'cognition', 'nextday'],
        'optional': [],
    },
}


# ---------------------------------------------------------------------------
# 重复检查规则（2026-09-16 维护新增，对应《规范_v2》「信息原子唯一落地」）
#
# 背景：五块是递进推导链，同一信息原子全篇只应完整落地一次、下游用指针。
# 但 2026-09-16 晨报实测：「3852 双向决策位」复述 22 处 / 13 个小节、
# 「15F55 3885.90」20 处 / 13 小节 —— 与规范严重背离，报告 3 天膨胀 2.9 倍。
# 根本原因：check_layout 只管「块在不在、顺序对不对」，完全不查重复 →
# 规范与产出之间没有机器拦网。本段补上这一环。
#
# 用法：check_layout.py 读取本配置，超阈值即 WARN 并打印行号。
#   仅对「当日报告」执行（历史报告是快照，重复检查无意义且会淹没信号）。
# ---------------------------------------------------------------------------
DUP_RULES = {
    # 价格关键位：同一个 4 位小数价格在全篇出现次数上限
    #   （关键位天然要引用几次：字段表 + 剧本 + 纪律 + 走势图标注，8 次算合理上沿）
    'price': {
        'label': '价格关键位',
        'pattern': r'\b3\d{3}\.\d{2}\b',
        'max': 8,
        'hint': '同一价位建议只在「关键位表」完整落地一次，其余处改指针（如「见关键位表」）',
    },
    # 核心结论短语：这些是「结论层」的东西，不该在每个小节都复述
    'phrase': {
        'label': '结论短语',
        'max': 8,
        'phrases': ['诱多', '暴力阳线', '双向决策位', '极度收口', '墨迹'],
        'hint': '结论短语应只在唯一落地点完整表述，下游引用结论即可',
    },
    # 单一事件的事实说明（如 Cookie 失效）重复次数上限
    'event': {
        'label': '事件说明重复',
        'max': 4,
        'phrases': ['401', 'Cookie 通道', 'zsxq_cookie'],
        'hint': '同一事件说明（如 Cookie 失效）应在报告头提示一次、附录表内一次即可',
    },
    # 「字段表 ↔ 关键位表」结构性双写检测
    'structural': {
        'label': '字段表与关键位表双写',
        # 若报告同时含「支撑」字段行（带 ≥2 个价格）与独立的「关键位」小节 → 疑似双写
        'field_row': r'\|\s*\*\*支撑\*\*\s*\|',
        'level_section': r'关键位.*(相对现价|百分比)',
        'hint': '「完整预判字段」的支撑/压力行与「关键位相对现价」表是同一数据两种排版，建议字段行改指针',
    },
}


def slot_order(tier):
    """返回该档位的规范顺序（required + optional 拼接）。"""
    if tier not in SLOTS:
        return []
    s = SLOTS[tier]
    return s['required'] + s['optional']


# ---------------------------------------------------------------------------
# 篇幅长度约束（《规范_v2》3.8）—— 补齐「规范在、无拦网」的最后一处缺口
#
# 背景：3.8（核心速览每行≤90 / 第一块≤5,000 / 一句话预判≤150 / 剧本触发条件≤80 /
# 纪律与观察合并为一节）自 2026-09-16 就写进规范，但 check_layout 一直不检查它。
# 结果 2026-09-16 报告核心速览三行为 195/224/108 字、第一块 8,492 字，
# 全部超限却「通过」；2026-09-17 首版同样超限。
# ——与 DUP_RULES 当初的处境一模一样：规范写在文档里，产出与规范之间没有机器拦网。
# 本段补上。计数口径：去掉全部空白后的字符数（中文按 1 计，标记符号按 1 计）。
# ---------------------------------------------------------------------------
LENGTH_RULES = {
    # 核心主线速览：每一行（非空、非分隔线）去空白后 ≤ max
    'core_line': {
        'label': '核心速览每行', 'max': 90, 'unit': 'section_lines',
        'section': r'^##\s*核心主线速览',
        'hint': '核心速览只放结论，细节下沉到第一块（事实）与第二块（推导）',
    },
    # 第一块星球信息：整块去空白后 ≤ max
    'star_block': {
        'label': '第一块星球信息', 'max': 5000, 'unit': 'section_block',
        'section': r'^##\s*第一块',
        'hint': '第一块只做事实落地；超出说明把推导/结论也塞了进来',
    },
    # 一句话预判：该子标题后的首个非空引用行 ≤ max（必须"纯结论"，不复述三因子/剧本/纪律）
    'oneshot': {
        'label': '一句话预判', 'max': 150, 'unit': 'after_heading',
        'heading': r'^####\s*一句话预判',
        'hint': '一句话预判需≤150字纯结论，三因子/剧本概率/纪律不得在此复述',
    },
    # 剧本表「触发条件」列：每行单元格去空白后 ≤ max
    'scenario_trigger': {
        'label': '剧本触发条件', 'max': 80, 'unit': 'table_col',
        'row': r'^\|\s*\*\*[ABC]\s', 'col': 3,
        'hint': '触发条件≤80字，佐证用「+」串联关键词而非整句',
    },
    # 第四块内「纪律」「观察」子标题数量 ≤ 1（即必须合并为一节）
    'discipline_merged': {
        'label': '纪律与观察合并', 'max': 1, 'unit': 'heading_count',
        'section': r'^##\s*第四块', 'heading': r'^####\s*(?:关键操作)?(?:纪律|开盘后.*观察)',
        'hint': '关键操作纪律与开盘后重点观察必须合并为一节，不要拆成两个 ####',
    },
}
